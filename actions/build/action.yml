name: build-marp-mkdocs
author: laurent gil
description: Create a website containing mkdocs content and marp content

# inputs:
#   marp_input_path:
#     default: slidedecks/kubernetes-overview/README.md
#     required: false
#     description: Path to the marp files
#   marp_output_path:
#     default: docs/Kubernetes/kubernetes-overview/README.html
#     required: false
#     description: Output path of the marp file conversion

runs:
  using: "docker"
  image: "Dockerfile"
  # env:
  #   MARP_INPUT_PATH: ${{ inputs.marp_input_path }}
  #   MARP_OUTPUT_PATH: ${{ inputs.marp_output_path }}
  # args:
  # - 'bzz'
  # pre-entrypoint: 'setup.sh'
  # entrypoint: 'main.sh'
